# Jupyter environment for coursework (same class as presentation/).
# Install only inside an activated venv: python3 -m venv .venv && source .venv/bin/activate
jupyterlab==4.4.9
numpy==1.26.4
matplotlib==3.10.3
seaborn==0.13.2
missingno==0.5.2
